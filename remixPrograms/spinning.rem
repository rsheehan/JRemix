graphics : include graphics

using graphics
	window : simple window

	triangle : a triangle
	triangle's size : 100
	triangle's position : { 0, std height ÷ 2}
	add (triangle) to (window)

	rotate : every 1 tick do
		change (triangle) heading by 2 degrees

	move : every 1 tick do
		change (triangle) x-pos by 4

	animate 60 ticks per second
		(move) animation
		(rotate) animation
		update (window)
	… until
		triangle's x-pos ≥ std width

"Finished" ↲
