There is a problem with

using graphics lib
	window : a simple window

	shape : a square
	shape's size     : 100
	add (shape) to (window)
	update (window)

	turn : 45
	degree : 0
	animate 2 ticks per second
		degree ↲
		"window: ", window \n
		degree : degree + turn
	… until [degree ≥ 90]

Because the body of the animate routine is called in a separate thread

Now:
using graphics
	'window' : a graphics window
	'shape' : a square
	'shape' 'size' : 100
	place ('shape') in ('window' next layer)

	'turn' : 3
	animate at 1 tick per second
		change 'shape' heading by 'turn' degrees
		update 'window'
		'degree' : ('shape' heading) radians as degrees
		'degree'↲
	… until ['degree' ≥ 90]

=======
When doing
	using turtle
    	help "group"

The last two methods don't pick up the function documentation:

Method: hide (OBJECT)
Method: (OBJECT) speed (new speed)

Could be associated with the fact these methods match both for turtles and turtle groups.

=======
If a deletion leaves an error style on a tab and then the tab is moved to start a line it keeps the error style.
The auto indent is adding tabs along with any existing ones. Not a big problem, just not nice.
Still some problems of not lexing through lines which were multiline strings but aren't currently.